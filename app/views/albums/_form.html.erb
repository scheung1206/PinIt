<%= simple_form_for([@user, @album]) do |f| %>
  <%= f.error_notification %>
<h3>New Album</h3>
  <div class="form-inputs">
    <%= f.input :title %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
