<%= simple_form_for([@pin, @comment]) do |f| %>
  <%= f.error_notification %>
<h3>New Comment</h3>
  <div class="form-inputs">
    <%= f.input :content %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
